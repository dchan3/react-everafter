import { default as Paginator_ } from './components/Paginator';
import { default as TablePaginator_ } from './components/TablePaginator';

var EverAfter = {
  Paginator: Paginator_,
  TablePaginator: TablePaginator_
}

export default EverAfter;
export const Paginator = Paginator_;
export const TablePaginator = TablePaginator_;
