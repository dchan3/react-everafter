import { default as Paginator } from './components/Paginator';
import { default as TablePaginator } from './components/TablePaginator';

var EverAfter = {
  Paginator, TablePaginator
}

export const Paginator = Paginator;
export const TablePaginator = TablePaginator;
export default EverAfter;
